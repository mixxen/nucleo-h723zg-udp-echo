[package]
name = "udp-benchmark"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
clap = { version = "4.5.47", features = ["derive"] }
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.143"
socket2 = "0.6.0"

[profile.release]
codegen-units = 1
lto = "thin"

